# Timeline - 21-06-2026

Hôm nay đã hoàn thành các công việc sau cho dự án Hệ thống Quản lý Bệnh viện (HMS):

## 1. Cập nhật Quy trình Làm việc (Workflow Rules)
- Bổ sung quy tắc 8 và 9 vào mục **Quy tắc khi agent làm việc trong repo** của file [AGENTS.md](file:///d:/QuanLyBenhVien/AGENTS.md) nhằm thiết lập quy trình tự động cập nhật nhật ký hoạt động (`timelines/DD-MM-YYYY.md`) và file [CHANGELOG.md](file:///d:/QuanLyBenhVien/CHANGELOG.md).

## 2. Sửa lỗi & Vận hành Thử nghiệm (FE & BE Verification)
- **Giải quyết xung đột Port**: Đổi cấu hình cổng mặc định trong [Properties/launchSettings.json](file:///D:/QuanLyBenhVien/Properties/launchSettings.json) thành HTTP `5233` và HTTPS `7233` để khắc phục lỗi port in use.
- **Sửa lỗi Query EF Core (SQLite)**: 
  - Khắc phục lỗi `InvalidOperationException` do SQLite không hỗ trợ dịch phương thức `string.Split` trong LINQ query tại `AuthController.Login`.
  - Sử dụng phương án thay thế bằng `.StartsWith` an toàn và tương thích hoàn toàn với EF Core.
- **Đăng nhập & Điều hướng vai trò Bác sĩ**: Sửa lỗi đường dẫn điều hướng của Bác sĩ từ `QueueController` (không tồn tại) sang `DashboardController` (`Index` action của Area `Doctor`).
- **Chạy thử toàn diện**: Khởi động server backend và giao diện frontend thành công. Sử dụng Browser Agent để kiểm thử tự động toàn bộ 3 phân quyền:
  1. **Admin Portal**: Đăng nhập nhanh -> Điều hướng đến `/Admin` -> Hiển thị Dashboard đầy đủ chỉ số -> Đăng xuất.
  2. **Doctor Portal**: Đăng nhập nhanh -> Điều hướng đến `/Doctor` -> Hiển thị Danh sách hàng đợi khám bệnh -> Đăng xuất.
  3. **Patient Portal**: Đăng nhập nhanh -> Điều hướng đến `/Patient` -> Hiển thị Dashboard bệnh nhân -> Đăng xuất.
- **Cải thiện Giao diện Đăng nhập**: Khắc phục lỗi lồng thẻ `<body>` không hợp lệ trong [Login.cshtml](file:///d:/QuanLyBenhVien/Views/Auth/Login.cshtml) bằng cách đổi thành thẻ `<div>` bọc ngoài có class flexbox căn giữa (`flex flex-col items-center justify-center min-h-screen`), giúp toàn bộ khung đăng nhập và demo credentials luôn hiển thị chính giữa màn hình theo cả 2 chiều trên mọi thiết bị.

