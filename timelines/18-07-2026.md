# 18-07-2026

- Sua loi cu phap CS1513 trong `Areas/Admin/Controllers/InvoicesController.cs` do dau cham thua sau `AsQueryable();`; xac nhan `dotnet build --no-restore` thanh cong voi 0 warning va 0 error.

- Sửa lỗi HTTP 500 tại `/Patient/Record` khi ứng dụng truy vấn bảng tài liệu bệnh nhân nhưng database cục bộ chưa áp dụng migration mới.
- Tự động chạy các EF Core migration còn thiếu khi ứng dụng khởi động, trước bước seed dữ liệu.
- Xác nhận migration `20260717073600_AddPatientDocuments` và kiểm tra build dự án.
- Đã thử nghiệm phương án OTP email SMTP; theo yêu cầu hiện tại, luồng đăng ký vẫn giữ OTP demo `123456`.
- Chuẩn hóa cuộn độc lập cho bảng ở Home/Patient/Doctor/Admin; loại bỏ cuộn dọc lồng nhau ở patient content, bổ sung vùng cuộn cho bảng dùng `overflow-auto` và ngăn lan truyền cuộn giữa các khối.
- Cố định vùng cuộn riêng cho bảng “Danh sách hóa đơn viện phí” để luôn có thanh cuộn trong card.
- Kiểm thử route chính của Admin/Bác sĩ/Bệnh nhân trên instance Visual Studio: 25 route trả 200; các API/ID không hợp lệ trả 400/404 đúng.
- Bổ sung chống CSRF cho xác nhận/hủy lịch, khóa/mở khóa bác sĩ, thu hóa đơn và gửi biên lai; chống xác nhận/thanh toán lặp và kiểm tra phương thức thanh toán/số lượng thuốc.
- Chuyển provider runtime sang Microsoft SQL Server LocalDB `(localdb)\\MSSQLLocalDB`; giữ SQLite provider để tương thích dữ liệu cũ và chuẩn bị migration SQL Server riêng.
- Đã restore provider SQL Server và build thành công; chưa thể tạo database LocalDB vì instance `MSSQLLocalDB` trên máy chưa khởi tạo/không truy cập được.
- Thêm lựa chọn provider theo cấu hình: Visual Studio mặc định SQL Server, Render hiện tại giữ SQLite qua `Database__Provider=Sqlite` để không làm hỏng deploy hiện có.
- Kiểm thử startup SQLite bằng database tạm theo cấu hình Render; EF Core áp dụng các migration thành công. LocalDB chưa thể cài tự động do máy yêu cầu phiên PowerShell Administrator và đang có pending reboot.
- Thêm provider `MySql.EntityFrameworkCore` 10.0.7; Visual Studio mặc định MySQL local ở cổng 3306, còn Render vẫn chọn SQLite qua biến provider.
- Tich hop Entity Framework Core voi MySQL (`MySql.EntityFrameworkCore`), them cau hinh `Database:Provider=MySql` va chuoi ket noi mau cho local; Render van giu SQLite demo va huong dan dung MySQL managed qua env vars.
- Them tai lieu `docs/DATABASE-MYSQL.md` huong dan cau hinh mat khau an toan va luu du lieu khi deploy Render.
- Kiểm thử và tái thiết kế trang đăng nhập theo chuẩn HIS hiện đại: bố cục split-screen, nhận diện bệnh viện, phân vai rõ ràng, responsive, focus keyboard, reduced-motion và tài khoản demo dạng disclosure; build thành công không lỗi.
- Sửa lỗi toàn bộ trang trả HTTP 500 do cấu hình mặc định ép MySQL với mật khẩu placeholder `CHANGE_ME`: local mặc định dùng lại SQLite `hms.db`; MySQL tiếp tục được hỗ trợ qua biến môi trường/user-secrets có connection string thật.
- Đồng bộ design system cho Home/Admin/Bác sĩ/Bệnh nhân theo màn hình đăng nhập: nền clinical teal/navy, sidebar/topbar, card, form, bảng dữ liệu, focus state, hover tinh tế và reduced-motion trong `wwwroot/css/site.css`.
- Tái thiết kế `Views/Auth/Register.cshtml`: bố cục onboarding hai cột, nhóm thông tin rõ ràng, chỉ dẫn 3 bước, ẩn/hiện mật khẩu, responsive mobile và trạng thái focus/accessibility; build xác nhận thành công.
- Đồng bộ lại `Views/Auth/VerifyOtp.cshtml` với luồng đăng ký mới, sửa JavaScript nằm ngoài thẻ script và bổ sung card OTP, validation số, resend state, mobile layout; build xác nhận thành công.
- Chuẩn hóa palette toàn hệ thống về xanh dương bệnh viện: sidebar/topbar/card/table/form và toàn bộ auth flow (Login/Register/OTP) dùng navy-blue làm màu chủ đạo, giữ focus/accessibility và reduced-motion.
