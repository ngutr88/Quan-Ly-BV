# Timeline - 23-06-2026

Hôm nay đã hoàn thành các công việc sau nhằm kiểm tra, sửa lỗi và tối ưu hóa toàn bộ các chức năng của Bệnh nhân (Patient):

## 1. Đồng bộ hóa Hồ sơ Người thân (Dependents) với Cơ sở dữ liệu
- **Tạo Model C#**: Định nghĩa lớp [Dependent.cs](file:///d:/QuanLyBenhVien/Models/Dependent.cs) đại diện cho bảng `NguoiThan` sẵn có trong SQLite.
- **Cập nhật Điều hướng**: Liên kết quan hệ 1-Nhiều từ `Patient` sang `Dependent` trong [Models/User.cs](file:///d:/QuanLyBenhVien/Models/User.cs) và [ApplicationDbContext.cs](file:///d:/QuanLyBenhVien/Data/ApplicationDbContext.cs).
- **Hiện thực hóa giao diện tĩnh**: Chuyển đổi trang [Dependents.cshtml](file:///d:/QuanLyBenhVien/Areas/Patient/Views/Record/Dependents.cshtml) từ hiển thị mock UI thành nạp dữ liệu động từ DB.
- **Tạo API lưu trữ**: Bổ sung action `AddDependent` sử dụng AJAX POST kết hợp mã xác thực CSRF Token giúp bệnh nhân lưu mới hồ sơ người thân trực tiếp vào DB và reload cập nhật giao diện.
- **Seeder mẫu**: Cập nhật [DbSeeder.cs](file:///d:/QuanLyBenhVien/Data/DbSeeder.cs) để tự động nạp sẵn 2 hồ sơ người thân mẫu (Trần Minh Hải, Nguyễn Thị Hoa) cho tài khoản Bệnh nhân mặc định.

## 2. Tích hợp đặt lịch khám hộ cho người thân
- **Chọn bệnh nhân**: Thêm bộ chọn `<select>` động ở đầu form đặt lịch tại [Book/Index.cshtml](file:///d:/QuanLyBenhVien/Areas/Patient/Views/Book/Index.cshtml) cho phép lựa chọn khám cho bản thân hoặc cho người thân.
- **Nhận diện liên kết**: Thêm JS tự động chọn người thân tương ứng nếu URL chứa tham số `?dependent=Name` khi nhấn "Đặt lịch khám hộ" từ trang hồ sơ người thân.
- **Xử lý Lý do khám**: Cập nhật action `ConfirmBooking` trong [BookController.cs](file:///d:/QuanLyBenhVien/Areas/Patient/BookController.cs) tự động chèn thêm tiền tố `[Đặt lịch hộ: Tên người thân (Mối quan hệ)]` vào trường `LyDoKham` để hiển thị trực quan cho bác sĩ, giúp giữ nguyên cấu trúc database mà không cần thêm cột.

## 3. Sửa lỗi định tuyến & Tối ưu hóa đơn thuốc điện tử
- **Khắc phục lỗi PhieuKhamId**: Đổi điều kiện truy vấn đơn thuốc trong action `PrescriptionDetails` ([RecordController.cs](file:///d:/QuanLyBenhVien/Areas/Patient/Controllers/RecordController.cs)) từ `p.Id == id` thành `p.PhieuKhamId == id` để phù hợp với ID của phiếu khám được truyền sang từ danh sách điều trị.
- **Đồng bộ liên kết**: Đổi liên kết đơn thuốc gần đây trên Dashboard tại [Dashboard/Index.cshtml](file:///d:/QuanLyBenhVien/Areas/Patient/Views/Dashboard/Index.cshtml) thành `@pres.PhieuKhamId` để thống nhất cách truy vấn.
- **Ẩn liên kết rỗng**: Tải trước danh sách các phiếu khám có đơn thuốc thực tế đưa vào `ViewBag.RecordsWithPrescriptions`. Cập nhật [Record/Index.cshtml](file:///d:/QuanLyBenhVien/Areas/Patient/Views/Record/Index.cshtml) để ẩn nút "Đơn thuốc" hoặc hiển thị text thay thế khi phiên khám không có đơn thuốc, tránh gây ra lỗi 404.

## 4. Bổ sung chức năng Đánh giá Bác sĩ trực tuyến
- **Xây dựng API**: Thêm hai action `GetReview` và `SubmitReview` vào `RecordController.cs` để tải và lưu trữ thông tin đánh giá (số sao từ 1-5, nhận xét) cho bác sĩ điều trị.
- **Giao diện Modal popup**: Thêm nút "Đánh giá BS" cạnh mỗi phiên khám đã hoàn thành và thiết kế một Modal popup chấm sao trực quan trong `Record/Index.cshtml`. Sử dụng AJAX để gửi phản hồi lên server và hiển thị Toast thông báo thành công mà không cần tải lại trang.

## 5. Cải thiện thẩm mỹ Sổ sức khỏe điện tử (Health Profile)
- **Thông báo Empty State**: Bổ sung điều kiện kiểm tra dữ liệu sinh tồn trong [Record/Health.cshtml](file:///d:/QuanLyBenhVien/Areas/Patient/Views/Record/Health.cshtml). Khi chưa có dữ liệu đo từ bác sĩ, trang sẽ hiển thị một thông báo Empty State chuyên nghiệp thay vì hai biểu đồ trống.

## 6. Biên dịch hệ thống
- Chạy thử lệnh `dotnet build` và xác nhận hệ thống biên dịch thành công 100% với 0 lỗi.

## 7. Khắc phục lỗi khởi tạo cơ sở dữ liệu trên Visual Studio
- **Sửa lỗi schema mismatch**: Chuyển đổi phương thức khởi dựng DB trong [DbSeeder.cs](file:///d:/QuanLyBenhVien/Data/DbSeeder.cs) từ `context.Database.EnsureCreated()` sang `context.Database.Migrate()` để hỗ trợ tự động áp dụng các migrations khi thay đổi cấu trúc bảng (cụ thể là bảng `NguoiThan`).
- **Dọn dẹp DB cũ**: Xóa bỏ các tập tin SQLite cũ (`hms.db`) ở thư mục gốc và thư mục output `bin/Debug/net10.0/` để tránh xung đột khi chạy lần đầu.
- **Biên dịch & Khởi chạy**: Kiểm thử chạy lệnh `dotnet run` thành công, hệ thống tự động tạo lại tệp SQLite, chạy tất cả migrations (bao gồm cả bảng `NguoiThan` cho hồ sơ người thân) và hoàn thành nạp dữ liệu mẫu sạch sẽ.

## 8. Bổ sung trang Patient/Payment và Cổng thanh toán trực tuyến mô phỏng
- **Nâng cấp Cơ sở dữ liệu (Database Schema)**: Bổ sung thuộc tính `MaGiaoDich` (nullable string) vào thực thể [Invoice](file:///d:/QuanLyBenhVien/Models/Billing.cs) (`HoaDon`). Tạo migration EF Core `AddTransactionCodeToInvoice` và cập nhật cơ sở dữ liệu SQLite thành công.
- **Dữ liệu mẫu (Database Seeding)**: Cập nhật [DbSeeder.cs](file:///d:/QuanLyBenhVien/Data/DbSeeder.cs) để tự động xóa sạch dữ liệu cũ và gán lại tập dữ liệu mẫu hóa đơn phong phú cho bệnh nhân Trần Văn A, bao gồm các trạng thái: Đã thanh toán, Chưa thanh toán, Quá hạn, Thất bại, Đang xử lý, Đã hủy để phục vụ kiểm thử.
- **Cải tiến Controller**: Nâng cấp [PaymentController.cs](file:///d:/QuanLyBenhVien/Areas/Patient/Controllers/PaymentController.cs) hỗ trợ:
  - AJAX API `SendEmailReceipt` giả lập gửi biên lai qua email và ghi nhận audit logs / notifications.
  - Action `Simulate` và callback `PaymentCallback` hỗ trợ mô phỏng 3 trạng thái kết quả thanh toán (Thành công, Thất bại, Đang xử lý) dựa trên phương thức đã chọn.
- **Giao diện Hóa đơn & Thanh toán**: Hiện thực hóa trang [Index.cshtml](file:///d:/QuanLyBenhVien/Areas/Patient/Views/Payment/Index.cshtml) với:
  - Dashboard tài chính: 4 thẻ KPI thống kê tổng nợ, tổng đã thanh toán, số hóa đơn nợ và trễ hạn.
  - Bộ lọc thông minh: tìm theo mã hóa đơn, khoảng thời gian ngày tạo, trạng thái thanh toán, phương thức thanh toán.
  - Sidebar chi tiết: Hiển thị đầy đủ thông tin khoa, bác sĩ, lịch khám, ngày khám, phương thức thanh toán, mã giao dịch, thời gian thanh toán và breakdown viện phí chi tiết.
  - Tích hợp in hóa đơn trực tiếp từ trình duyệt (`window.print()`) và tải PDF chất lượng cao sử dụng thư viện `html2pdf.js` từ CDN.
  - Tích hợp gửi email nhận biên lai AJAX hiển thị Toast thông báo thành công.
  - Nút "Thanh toán" và "Thanh toán lại" động dẫn tới cổng thanh toán tương ứng.
  - Thêm danh sách Lịch sử giao dịch trực tuyến ở phía dưới.
  - Hỗ trợ Empty State đẹp mắt khi không tìm thấy hóa đơn nào đi kèm nút CTA đặt lịch khám.
- **Cổng thanh toán Mô phỏng**: Hiện thực hóa [Simulate.cshtml](file:///d:/QuanLyBenhVien/Areas/Patient/Views/Payment/Simulate.cshtml) hiển thị giao diện tùy biến theo thương hiệu của phương thức được chọn (VNPay, MoMo, ZaloPay, Chuyển khoản ngân hàng, Tại quầy) và cho phép bệnh nhân bấm mô phỏng kết quả giao dịch tuỳ ý.

## 9. Mở rộng dữ liệu mẫu khoa và bác sĩ; sửa dropdown trang Create bác sĩ

### Mở rộng Seed Data ([DbSeeder.cs](file:///d:/QuanLyBenhVien/Data/DbSeeder.cs))
- **Thêm 4 khoa mới** (tổng 8 khoa): Khoa Ngoại tổng hợp, Khoa Sản phụ khoa, Khoa Da liễu, Khoa Thần kinh — với thông tin mô tả và vị trí phòng khám đầy đủ.
- **Thêm 4 bác sĩ mới** (tổng 6 bác sĩ): BS. Phạm Đức Hùng (Nội tổng quát), BS. Vũ Thị Hương (Tai Mũi Họng), BS. Trần Minh Khoa (Ngoại - PGS.TS.BS), BS. Nguyễn Thị Lan Anh (Da liễu), mỗi người với chuyên khoa, học vị và lịch làm việc riêng.
- **Thêm bệnh nhân thứ 3**: Lê Văn Cường với tiền sử bệnh Viêm gan B mãn tính để phong phú dữ liệu demo.
- **Thêm auto-patch logic (non-destructive)**: Seeder tự động detect và bổ sung các khoa còn thiếu vào DB đã tồn tại mà không xóa dữ liệu cũ, đảm bảo dropdown khoa luôn đầy đủ ngay cả khi người dùng nâng cấp từ phiên bản trước.

### Sửa lỗi dropdown Khoa ([Areas/Admin/Views/Doctors/Create.cshtml](file:///d:/QuanLyBenhVien/Areas/Admin/Views/Doctors/Create.cshtml))
- **Nguyên nhân lỗi**: Trang Create không có `@model` directive nên `asp-items` tag helper không bind được `ViewBag.KhoaId` — dropdown hiển thị rỗng.
- **Cách sửa**: Thay `<select asp-items="ViewBag.KhoaId">` bằng `@Html.DropDownList("khoaId", ViewBag.KhoaId as IEnumerable<SelectListItem>, ...)` — cách Razor helper này render đúng không phụ thuộc vào `@model`.



