# Nhật ký công việc ngày 31-07-2026

## Giữ trạng thái bác sĩ trên một hàng

- Thay đổi `Areas/Admin/Views/Departments/Index.cshtml`.
- Nguyên nhân: chiều rộng cột trạng thái hẹp khiến nhãn `Hoạt động` bị ngắt thành hai dòng.
- Cách xử lý: thêm `inline-flex`, căn giữa theo chiều dọc và `whitespace-nowrap` cho cả nhãn `Hoạt động` lẫn `Đã khóa`.
- Kiểm chứng: lần build đầu không khởi chạy được compiler vì thư mục tạm hệ thống hết dung lượng. Sau khi chuyển `TEMP`/`TMP` của riêng lệnh kiểm tra sang `.tmp/msbuild-temp`, `dotnet build QuanLyBenhVien.csproj --no-restore -p:UseAppHost=false -o .tmp\department-status-build-check` thành công với 0 cảnh báo và 0 lỗi; `git diff --check` không phát hiện lỗi whitespace.
