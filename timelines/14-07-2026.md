# Timeline - 14/07/2026

## Đồng nhất UI Components: Sidebar, Topbar và Logo

### Mục tiêu
Đồng nhất giao diện các thành phần chung (sidebar, topbar, logo) trên 3 layout chính của hệ thống để tạo trải nghiệm nhất quán cho người dùng.

### Các thay đổi thực hiện

#### `Views/Shared/_AdminLayout.cshtml`
- **Logo**: Đồng nhất icon thành `medical_services` (FILL), text `MediFlow HMS`, subtitle `Quản trị hệ thống`, logo có thể click về Dashboard
- **Logo animation**: Thêm `group-hover:scale-105 transition-transform` khi hover
- **Sidebar nav**: Bổ sung thêm 2 mục `Nhân sự & Phân quyền` (admin_panel_settings) và `Báo cáo & Thống kê` (analytics) phù hợp với design file 05, 27, 26
- **Bottom nav**: Thêm border-top phân cách, thêm `Cấu hình hệ thống` (settings) vào bottom nav, thêm active state cho Logs và Settings
- **Bác sĩ icon**: Đổi `medical_services` thành `stethoscope` cho nav item Bác sĩ để phân biệt với logo
- **Topbar**: Bổ sung notification bell button, thêm divider, avatar dùng ký tự đầu tên thay vì hard-code `A`
- **Avatar**: Avatar lấy ký tự đầu của `User.Identity.Name` thay vì hard-code
- **Footer**: Đồng nhất format `© 2026 HMS MediFlow • Trạng thái hệ thống: <span>Ổn định</span>`

#### `Views/Shared/_DoctorLayout.cshtml`
- **Logo**: Đồng nhất icon thành `medical_services` (FILL) màu `secondary`, text `MediFlow HMS`, subtitle `Khu vực bác sĩ`, logo clickable
- **Logo animation**: Thêm hover scale animation nhất quán với Admin layout
- **Sidebar nav**: Bổ sung mục `Thống kê cá nhân` (analytics) phù hợp với design file 30
- **Bottom nav**: Thêm border-top phân cách, đồng nhất style với Admin layout
- **Topbar**: Thêm search box (focus ring màu secondary), thêm live date display (Vietnamese), thêm notification bell, thêm divider, đồng nhất avatar
- **Avatar**: Dùng `secondary-container` background màu `secondary`, lấy ký tự đầu tên thay vì hard-code `Dr`
- **Script**: Thêm inline script hiển thị ngày hiện tại (tiếng Việt) trong topbar
- **Footer**: Đồng nhất format `© 2026 HMS MediFlow • Trạng thái: <span>Hoạt động tốt</span>`

#### `Views/Shared/_PatientLayout.cshtml`
- **Logo**: Đồng nhất icon thành `medical_services` (FILL), text `MediFlow HMS`, thêm subtitle `Cổng bệnh nhân` (ẩn trên mobile), logo clickable với hover animation
- **Desktop navbar**: Đổi `rounded` thành `rounded-lg` nhất quán với các layout khác
- **Dropdown menu**: Cải thiện với header info (tên + "Bệnh nhân"), thêm icon Material Symbols cho từng mục, cải thiện padding và border-radius
- **Mobile tab bar**: Thêm active state với `FILL` icon khi tab đang active (Dashboard, Book, Record, Payment, Notification)
- **Avatar**: Lấy ký tự đầu của `User.Identity.Name` thay vì hard-code `BN`
- **Footer**: Giữ nguyên format phù hợp với Patient portal

### Nguyên tắc đồng nhất đã áp dụng

| Thành phần | Admin | Doctor | Patient |
|---|---|---|---|
| Logo icon | `medical_services` FILL | `medical_services` FILL | `medical_services` FILL |
| Logo text | MediFlow HMS | MediFlow HMS | MediFlow HMS |
| Logo subtitle | Quản trị hệ thống | Khu vực bác sĩ | Cổng bệnh nhân |
| Logo color | `primary` | `secondary` | `primary` |
| Logo animation | hover scale | hover scale | hover scale |
| Active state color | `primary` | `secondary` | `primary` |
| Avatar background | `primary-fixed` | `secondary-container` | `primary-fixed` |
| Notification bell | ✅ | ✅ | ✅ (với badge) |
| Footer format | `© 2026 HMS MediFlow` | `© 2026 HMS MediFlow` | `© 2026 HMS MediFlow` |

## Cập nhật bổ sung (14/07/2026)

### 1. Đồng bộ màu sắc trang Bác sĩ (Doctor views)
- Đổi toàn bộ các thành phần màu xanh lá (`secondary`) không cần thiết sang xanh dương (`primary`) giống Admin (các nút chính, badge mã ca, welcome banner, BMI display, v.v.).
- Giữ lại màu xanh lá (`secondary`) cho các badge trạng thái y tế như "Đã hoàn thành" hoặc trạng thái "Đang trực tuyến" trong Chat để đảm bảo trực quan nghiệp vụ.

### 2. Thống kê cá nhân Bác sĩ (`/Doctor/Stats`)
- Tạo Controller [`StatsController.cs`](file:///d:/QuanLyBenhVien/Areas/Doctor/Controllers/StatsController.cs) và View [`Index.cshtml`](file:///d:/QuanLyBenhVien/Areas/Doctor/Views/Stats/Index.cshtml).
- Thống kê các chỉ số: KPIs (hôm nay, tháng này, năm nay, tổng lượt khám), biểu đồ lượt khám 6 tháng gần nhất (CSS thuần), phân bổ sao đánh giá bệnh nhân, top bệnh nhân tái khám nhiều nhất và các nhận xét đánh giá gần đây.

### 3. Ẩn thanh tìm kiếm trên Header Admin (`_AdminLayout.cshtml`)
- Ẩn thanh tìm kiếm mockup tĩnh trên Topbar của Admin Layout theo mặc định (chỉ hiển thị khi `ViewData["ShowTopSearch"] == true`).
- Lý do: Tránh dư thừa và gây nhiễu vì các trang quản lý của Admin (Bệnh nhân, Bác sĩ, Thuốc, Hóa đơn, Logs...) đều đã có sẵn thanh tìm kiếm và bộ lọc chuyên biệt, tối ưu trong phần nội dung chính của trang.
